# =============================================================================
# CEW AI Service Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# Required fields are marked with [REQUIRED]
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Port number for the HTTP server
# Default: 3001
PORT=3001

# Environment mode: development | production | test
# Affects logging verbosity and error details
NODE_ENV=development

# -----------------------------------------------------------------------------
# OpenAI Configuration [REQUIRED]
# -----------------------------------------------------------------------------

# OpenAI API key for embeddings and chat completions
# Get your key from: https://platform.openai.com/api-keys
# [REQUIRED] - The service will not start without this
OPENAI_API_KEY=sk-your-openai-api-key-here

# Model for chat completions (question answering)
# Options: gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo
# Default: gpt-4-turbo-preview
OPENAI_MODEL=gpt-4-turbo-preview

# Model for generating text embeddings
# Options: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
# Default: text-embedding-3-small (1536 dimensions, good balance of cost/quality)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# -----------------------------------------------------------------------------
# LLM Settings
# -----------------------------------------------------------------------------

# Temperature for response generation (0.0 - 2.0)
# Lower = more deterministic/focused, Higher = more creative/varied
# Default: 0.3 (recommended for factual Q&A)
LLM_TEMPERATURE=0.3

# Maximum tokens in generated response
# Default: 2000
LLM_MAX_TOKENS=2000

# -----------------------------------------------------------------------------
# Document Processing
# -----------------------------------------------------------------------------

# Path to documents folder (absolute or relative to project root)
# Default: ./documents
DOCUMENTS_PATH=./documents

# Path to vector index storage (absolute or relative to project root)
# Default: ./index-store
INDEX_STORE_PATH=./index-store

# Maximum chunk size in characters
# Larger chunks = more context but less precise retrieval
# Default: 1000
CHUNK_SIZE=1000

# Overlap between adjacent chunks in characters
# Helps maintain context across chunk boundaries
# Must be less than CHUNK_SIZE
# Default: 200
CHUNK_OVERLAP=200

# Maximum file size allowed for upload (in MB)
# Default: 50
MAX_FILE_SIZE_MB=50

# -----------------------------------------------------------------------------
# Vector Store Configuration
# -----------------------------------------------------------------------------

# Vector store type: chroma | faiss
# Default: chroma
VECTOR_STORE_TYPE=chroma

# ChromaDB server hostname
# Default: localhost
CHROMA_HOST=localhost

# ChromaDB server port
# Default: 8000
CHROMA_PORT=8000

# ChromaDB collection name for storing document embeddings
# Default: cew_documents
CHROMA_COLLECTION=cew_documents

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------

# Log level: error | warn | info | debug
# Default: info
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Allowed origin for CORS (your frontend URL)
# Default: http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# -----------------------------------------------------------------------------
# CEW Integration (Optional)
# -----------------------------------------------------------------------------

# CEW backend API URL for QA/QC data access
# Leave empty if not using CEW integration
CEW_API_URL=http://localhost:5000

# API key for CEW backend authentication
# Leave empty if not using CEW integration
CEW_API_KEY=
