{
  "name": "cew-1",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix CEW/_root",
    "build": "npm run build --prefix CEW/_root",
    "preview": "npm run preview --prefix CEW/_root"
  }
}
