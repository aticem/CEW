{
  "name": "cew-1",
  "private": true,
  "scripts": {
    "dev": "npm --prefix CEW1/_root run dev",
    "dev:ai": "npm --prefix ai-service run dev",
    "build": "npm --prefix CEW1/_root run build",
    "build:ai": "npm --prefix ai-service run build"
  }
}
